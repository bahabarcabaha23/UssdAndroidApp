<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 🆕 أندرويد 14: مطلوب مع foregroundServiceType="specialUse" أدناه على HttpServerService -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- 🆕 لزر استثناء التطبيق من تحسين البطارية (مهم على سامسونج/One UI) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="USSD Companion"
        android:theme="@style/Theme.UssdCompanion">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".UssdAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!--
            🆕 أندرويد 14 (targetSdk 34): يلزم تحديد foregroundServiceType. اخترنا "specialUse"
            لأن هذه الخدمة (جسر HTTP محلي يتحكم فيه برنامج C# عبر adb forward) لا تندرج تحت أي
            نوع قياسي (ليست dataSync سحابي تقليدي، ولا media/location/camera). خاصية
            PROPERTY_SPECIAL_USE_FGS_SUBTYPE إلزامية مع هذا النوع منذ API 34.
        -->
        <service
            android:name=".HttpServerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="local-http-bridge-controlled-via-adb-forward" />
        </service>

    </application>
</manifest>
